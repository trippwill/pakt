# Cloudflare Pages configuration
# Build command and output directory are configured in the CF Pages dashboard:
#   Build command: cd site && hugo --minify
#   Output directory: site/public
#   Environment variable: HUGO_VERSION = 0.147.1
#
# This file configures the static asset serving.

name = "pakt"
compatibility_date = "2026-04-04"

[assets]
directory = "./site/public"
